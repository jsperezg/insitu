<%= form_for @delivery_note_detail, 
			 url: new_user_delivery_note_delivery_note_detail_path(current_user, @delivery_note) do |f| %>
	<%= render partial: 'delivery_note_details/form',  locals: { f: f } %>
<% end %>
