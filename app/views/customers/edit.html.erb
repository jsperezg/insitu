<%- model_class = Customer -%>

<%= form_for @customer, url: user_customer_path(current_user.id, @customer), :html => { :class => "customer" } do |f| %>
  <%= render :partial => 'form', locals: { f: f } %>
<% end %>
