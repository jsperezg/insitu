<div class="box box-primary">
  <%= form_for @customer,  url: user_customers_path(current_user), html: { class: 'customer', role: 'form' } do |f| %>
    <%= render :partial => 'form', locals: { f: f } %>
  <% end %>
</div>
