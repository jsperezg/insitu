<%- model_class = Unit -%>
<div class="page-header">
  <h1><%=t 'units.create_title' %></h1>
</div>
<%= form_for @unit, url: user_units_path(current_user.id), :html => { :class => "unit" } do |f| %>
  <%= render :partial => 'form', locals: { f: f } %>
<% end %>
