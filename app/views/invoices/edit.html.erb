<%- model_class = Invoice -%>
<div class="page-header">
  <h1><%=t 'invoices.edit_title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>

<%= form_for @invoice, url: user_invoice_path(current_user, @invoice) do |f| %>
  <%= render :partial => 'form',  locals: { f: f } %>
<% end %>
