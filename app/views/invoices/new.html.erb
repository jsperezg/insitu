<%- model_class = Invoice -%>
<div class="page-header">
  <h1><%=t 'invoices.create_title', :default => [:'helpers.titles.new', 'New %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>

<%= form_for @invoice, url: user_invoices_path(current_user) do |f| %>
  <%= render :partial => 'form',  locals: { f: f } %>
<% end %>
