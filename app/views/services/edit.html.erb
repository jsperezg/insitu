<%- model_class = Service -%>

<%= form_for @service, url: user_service_path(current_user.id, @service), :html => { :class => "service" } do |f| %>
  <%= render :partial => 'form', locals: { f: f } %>
<% end %>
